@model projeto_pet_shop_bd1_2021.Models.ViewModels.FuncionarioViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Funcionario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Funcionario.PessoaId" class="control-label"></label>
                <select asp-for="Funcionario.PessoaId" class="form-control" asp-items="@(new SelectList(Model.Pessoa,"Id", "Nome"))"></select>
            </div>
            <div class="form-group">
                <label asp-for="Funcionario.CarteiraTrabalho" class="control-label"></label>
                <input asp-for="Funcionario.CarteiraTrabalho" class="form-control" />
                <span asp-validation-for="Funcionario.CarteiraTrabalho" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
